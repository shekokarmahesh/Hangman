{
  "name": "hangman-github",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "npm install concurrently --save-dev && cd Client && npm install && cd ../Server && npm install",
    "start": "concurrently \"cd Client && npm run dev\" \"cd Server && npx json-server DB.json\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
